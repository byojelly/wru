#!/usr/bin/env ruby

require "bundler/setup"
require "wru"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start


require "irb"
IRB.start


#belo was an attempt to setup a reload method so that in terminal i could just type reload after modifuing code. did not work. look up later

#def reload! #this method lets us easily reload our terminal rather than exiting
#  load "./lib/wru/cli.rb"
#  load "./lib/wru/match.rb.rb"
#end
